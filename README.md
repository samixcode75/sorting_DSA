# Selection Sort using Templates (C++)

## Description

This project implements the **Selection Sort algorithm** in C++ using **templates**.
The template function allows the sorting algorithm to work with different data types such as **integers and strings**.

Selection Sort works by repeatedly finding the **smallest element in the unsorted portion of the array** and swapping it with the first unsorted element. This process continues until the entire array is sorted.

## Features

* Implementation of **Selection Sort**
* Uses **C++ templates** for generic programming
* Works with multiple data types (e.g., `int`, `string`)
* Includes a function to **print arrays**

## File

* `Task_1.cpp` – Contains the implementation of the selection sort algorithm and test cases.

## How It Works

1. The algorithm scans the array to find the **smallest element**.
2. The smallest element is **swapped with the current index**.
3. The process repeats for
# sorting_DSA
